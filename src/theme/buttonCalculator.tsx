import {
    StyleSheet,
} from 'react-native';

const styles = StyleSheet.create({
    button: {
        borderRadius: 100,
        height: 80,
        justifyContent: 'center',
        marginHorizontal: 10,
        width: 80,
    },
    buttonText: {
        fontSize: 24,
        fontWeight: '300',
        padding: 10,
        textAlign: 'center',
    },
});

export default styles;
